# Local development (default): http://localhost:3000
#
# When exposing the Expo web app through ngrok, point this at the API tunnel.
# This overrides the built-in current ngrok fallback and should be changed each
# time ngrok gives the backend a new URL.
EXPO_PUBLIC_API_BASE_URL=https://oppressed-vertical-semicolon.ngrok-free.dev
