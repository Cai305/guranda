// Feature flags — flip these to enable/disable whole features without
// touching the code that implements them.

// AI companion (setup, guided tour, chat, mini app card).
// Set to true to re-enable the full AI journey.
export const AI_ENABLED = true;
